<section id="banner" role="region" aria-label="Banner principal">
    <div class="bg-image" aria-hidden="true"></div>
    <div class="content-overlay">
        <div class="content-wrapper">
            <div class="content">
                <h1>Tu negocio es digital. Tu abogado también debería serlo.</h1>
                <p>Somos abogados especialistas en nuevas tecnologías, protección de datos, comercio electrónico, marketing y reputación online.</p>
                <div class="banner-buttons">
                    <a href="/#contact" class="style1" aria-label="Solicitar una consulta legal">Solicitar una consulta</a>
                    <a href="/#services" class="style2" aria-label="Conocer nuestros servicios legales">Nuestros servicios</a>
                </div>
            </div>
        </div>
    </div>
</section>


<style>

    #banner{
        scroll-margin-top: 120px; /* ajusta según la altura de tu header */

        .bg-image{
            position: absolute;
            content: "";
            height: 100%;
            width: 100%;
            transition: all 0.3s ease-in-out;
              /* Fondo degradado base: de #f0f5f0 a white */

  /* Imagen encima */
  background-image: url('/images/person-cafe-black-and-white-white-socializing-restaurant-134-pxhere.com.jpg');
  background-position: right center;
  background-size: cover;
  background-repeat: no-repeat;
  background-blend-mode: normal;

  /* Difuminado de la imagen hacia la izquierda */
  mask-image: linear-gradient(to left, black 20%, transparent 40%);
  -webkit-mask-image: linear-gradient(to left, black 20%, transparent 40%);
        }

        position: relative;

        .content-overlay{
            padding: 100px 0px;
            background: linear-gradient(to right, #f0f5f0de, rgba(255, 255, 255, 0.589));

            display: flex;
            justify-content: center;
            width: 100%;
            height: 100%;

            .content-wrapper{
                display: flex;
                align-items: center;
                position: relative;
                width: 100%;
                padding: 0px 20px;
                max-width: 1300px;
                box-sizing: border-box;

                .content{
                    display: flex;
                    flex-direction: column;
                    justify-content: center;
                    max-width: 45%;
                    padding: 20px;
                    gap: 15px;

                    h1{
                        font-size: 60px;
                        margin: 0;
                    }

                    p{
                        font-weight: bold;
                        color: #5a6b5a;
                        font-size: 20px;
                    }
                    .banner-buttons{
                        display: flex;
                        align-items: center;
                        gap: 20px;

                        a{
                        border-radius: 0.5rem;
                        text-decoration: none;
                        color: #2e3c2e;
                        padding: 12px 22px;
                        text-align: center;
                        vertical-align: text-bottom;
                        font-weight: 600;
                        transition: all 0.3s ease-in-out;
                        outline: none;
                        border: 0;
                        cursor: pointer;
                        text-decoration: none;
                        }

                        .style1{
                            background: linear-gradient(135deg, #4d8b6c 0%, #6bbd97 100%);
                            color: #ffffff;
                            box-shadow: 
                                0 4px 12px rgba(77, 139, 108, 0.2),
                                0 2px 4px rgba(77, 139, 108, 0.1);

                            
                         }

                        .style1:hover{
                            background-color: #3d6e55;
                            color: white;
                        }

                        .style2{
                            border: 1px solid #4d8b6c;
                            background-color: white;
                            color: #4d8b6c;
                        }

                        .style2:hover{
                            background-color: #d5e3db
                        }
                    }
                }
            }

            h1{
            animation: slide-up 1s ease-out;
            font-size: 2.25rem;        /* text-4xl */
            font-weight: 700;          /* font-bold */
            letter-spacing: -0.015em;  /* tracking-tight */
            color: #2e3c2e;
            }
        }
    }

    /* Media Queries */
    @media (max-width: 1400px) {
    #banner .bg-image{
        mask-image: linear-gradient(to left, black 20%, transparent 45%);
        -webkit-mask-image: linear-gradient(to left, black 20%, transparent 45%);
    }

    @media (max-width: 1000px) {
    
    }

    @media (max-width: 900px) {
        #banner .bg-image{
            mask-image: linear-gradient(to left, black 0%, transparent 60%);
            -webkit-mask-image: linear-gradient(to left, black 0% transparent 60%);
        }

        #banner .content-overlay .content-wrapper .content{
            max-width: 60%;
        }
    }

    @media (max-width: 650px) {
        #banner .bg-image{
            mask-image: linear-gradient(to left, black -400px, transparent 40%);
            -webkit-mask-image: linear-gradient(to left, black -400px, transparent 40%);
        }

        #banner .content-overlay .content-wrapper .content{
            max-width: 100%;
        }

        .banner-buttons{
            flex-direction: column;
            a{
                width: 100%;
            }
        }
    }

    @media (max-width: 500px) {
        #banner .bg-image{
            mask-image: linear-gradient(to left, black -400px, transparent 40%);
            -webkit-mask-image: linear-gradient(to left, black -400px, transparent 40%);
        }

        #banner .content-overlay .content-wrapper .content{
            max-width: 100%;
        }

        h1{
            font-size: 40px !important;
        }

        p{
            font-size: 16px !important;
        }
    }

</style>